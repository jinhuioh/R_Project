##source('함수모음.R')를 하고 여기서 위에 있는 함수들을
##실행시키면 잘 작동하는 것을 확인 할 수 있다.
input.result = input.fun()
print(input.result)


##삼항연산자 true면 우수, false면 보통 출력
a_person <- 200
b_person <- 100

result5 <- ifelse(a_person > b_person, '우수', '보통')
print(result5)

